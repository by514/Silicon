# 音乐播放器（Music Player）

玩家随身 UI 的音乐播放器（非方块，不影响旧存档方块 ID）。支持内置原版音乐、网络 URL、本地文件三种曲目，
声源随播放者移动，**其他玩家按自己到播放者的距离远近听到**（多人 3D 声场）。

## 入口（三种都要）

1. **设置菜单**：Silicon 设置分类 → 「音乐播放器」按钮
2. **快捷键**：默认 `F9`，按下直接弹出播放器弹窗
3. **常驻悬浮条**：屏幕右下角音符按钮 `♪`，点击展开为迷你控制条（播放/暂停、曲名、下一曲、收起）；**可拖动到任意位置**（收起态整条可拖、展开态前端把手拖动），位置跨会话记忆

## 播放器功能（完整播放器）

- 播放 / 暂停 / 停止
- 上一曲 / 下一曲
- 音量（0~100%）
- 音高（0.5x~2.0x）
- 循环模式：关闭 / 列表循环 / 单曲循环 / 随机（队列乱序不重播）/ 单曲播完即停 / 真随机（共 6 种）
- 曲目列表管理：点击即播、移除曲目
- 添加曲目：游戏内置 / 网络 URL / 本地文件路径（Soloud 仅内置解码 ogg/mp3/wav；flac/m4a 等允许导入与多人共享，本机不可解码播放）

## 曲目类型

| 类型 | 来源 | 共享方式 |
|------|------|---------|
| INTERNAL | 游戏内置原版音乐（Musics.*：game1~9、boss1/2、fine、editor、menu、land、launch） | 所有人本地都有，广播元数据即可播放 |
| URL | 网络音频地址（.ogg/.mp3/.wav） | 播放者广播 URL，其他客户端下载到本地缓存后播放 |
| LOCAL | 播放者本地磁盘路径 | 播放者读取文件字节，二进制分块广播，其他客户端重组写缓存后播放 |

## 多人 3D 声场

- **播放者（owner）**：本人正常播放，声源以其世界坐标为原点。
- **其他玩家**：以「自己到 owner 的距离」做 3D 衰减，越近越清晰，超过衰减半径基本听不见。
- **可叠加**：多个玩家同时播放时，各声源独立叠加，互不冲突。
- **链路**：播放者 → 服务端 → 广播所有客户端 → 各端本地 3D 定位播放。
- 「启用」开关：关闭时本机不能播放，同时不接收/听不到别人的播放（双生效）。

## 本地缓存优先复用

- URL 下载、二进制广播收到的文件均缓存到 `cache/music/<hash>.<ext>`（保留原始扩展名，供 Soloud 按扩展名解码）。
- 再次播放（含他人播放同源曲目）时命中缓存直接复用，不再走网络。
- 缓存文件以内容 hash 命名，天然去重；跨重启也能通过 `<hash>.*` 扫描命中。
- 首帧共享：URL/网络播放时元数据携带 `ext`、`x`/`y`，让对端即时定位声源并正确解码。

## 网络注册时机

- `netClient`/`netServer` 在 mod init 时可能尚未创建，因此包处理器延迟到对应端加载完成事件注册：
  - 客户端：`ClientLoadEvent` 注册 `MusicNetwork` 的接收/发送回调
  - 服务端：`ServerLoadEvent` 注册转发回调（客户端上报 → 广播所有客户端）
  - 均带防重入标志，只注册一次
- 玩家离开（`PlayerLeave`）时清理其声源、坐标与接收缓冲，避免脱离后声源残留。

## 版本历史

| 版本 | 变更 |
|------|------|
| （待定版） | **悬浮窗布局与按钮文案、外部暂停再播放、音量 dB 显示一轮 UI 收敛**：①**循环/倍速按钮「越点越小」与文字被省略**——按钮 `pack()` 因长文案自适应而缩放；悬浮条循环/倍速按钮与弹窗循环按钮改为**固定宽度**（64/76/84px）并 `ellipsis(false)`+字缩 0.9，短文案不再收缩、长文案不再省略；②**悬浮窗歌名过长不循环反而超出条右沿**——展开态悬浮条改**固定整条宽度**（`Scl.scl(600)`）而非 `pack()` 自适应：原先 `pack()` 使 colspan 行的 `this.width==pref` → `getPrefWidth()>this.width+8` 恒 false → 永不滚动、文本漏出条右沿；固定宽后曲名/进度行以 colspan 铺满、长曲名在条内滚动剪裁；③**循环文案**去掉「循环：」前缀、六模式改两字等长（关闭/列表/单曲/乱序/单停/随机；en：Off/List/One/Shuf/Stop/Rand）；④**外部（URL/流式）暂停再播放跳到内置曲**——`resume()` 对暂停前确在播放的曲给 **3s 延长加载确认窗口**（`resumeGraceUntil`：慢速外部声源尚未回到播放态时不被「从未播放→停止」守卫在 0.5s 内误杀），窗口过期仍未恢复则停、不会自动跳到内置/其它曲（`autoAdvance` 只在自然播完/AUTO 时走），`play()`/`stopLocal()` 清除该窗口与 `wasPlayingBeforePause`；⑤音量显示改 **dB 对数**（`20·log10(volume)`：0.001→-60dB、0.1→-20dB、1→0dB、10→+20dB），滑杆本就对数，等长位移=等幅 dB 步进，直观体现响度对数增长 |：崩溃 `hs_err` 栈为 `arc.audio.Soloud.idSeek → MusicPlayer$SoloudBridge.seek → MusicPlayer.seek ← MusicPlayerDialog$rebuild`（读地址 0x0，`_thread_in_native` main）。UI 进度滑杆 `changed()` 直接调 `MusicPlayer.seek()`，`seek()` 原对 `localVoiceId>=0` 一律同步 `Soloud.idSeek`——刚 `beginPlayback` 创建、尚未确认存活（未 `isPlaying` / 建源不足 0.3s）的流式声源立即 idSeek 触发原生空指针（与已记录的 `resume()` 崩溃同族，但漏了滑杆这条路径）。修复：`seek()` 仅在「声源确认存活（`isPlaying` 且距建源 ≥0.3s）」时才同步 idSeek，否则走 `deferSeek(pendingResumeSeek)` 由 `tickLocal` 待声源稳定后延迟应用——杜绝 UI 滑杆/自动推进/倒放任意路径对新声源同步 idSeek；倒放的每帧反射 seek 也因此更安全 |
| （待定版） | **外部音频格式嗅探健壮性 + 音量削波封顶 + 超大文件仍显示时长**：①`sniffExt` 探测窗口由 4KB 扩到 1MB，并支持 **ID3v2 大 tag（含封面可达数百 KB）**——先按同步安全尺寸跳过整段 `ID3` 头再到音频区找 MPEG 帧同步，不再因 tag 内找不到帧同步而误判 null → 带封面/大 tag 的 mp3、无扩展名/后缀不符的 URL 也能正确落为 `.mp3`（`localAsciiCopy`/`sniffFileExt`/`writeCacheBytes` 同步扩窗）；②**1000% 音量直接×10 灌 Soloud 会硬削波破音**：新增 `effectiveVolume()` 应用封顶 `MAX_EFF_VOL=2.5`（远程衰减 `calcListenVolume` 同封顶），UI 仍保留 0–1000% 滑杆语义但不至于超倍失真毁听感；③**本地/URL/内置曲目时长探测上限 64MB 过严**：`Music.create` 只解 header 不求整文件长度，放宽 `readLengthFrom` 上限到 512MB（30–60 分钟常见音频不再误判 `--:--`）；整文件磁盘拷贝（`localAsciiCopy`）仍受 64MB 限制以避卡顿 |
| （待定版） | **外部（URL/流式）音频与本地曲目可播/可显时长三项根因修复**：①缓存查找改「磁盘事实优先」——`resolveExt` 改为先扫缓存目录 `<hash>.*` 再取登记值（URL 无扩展名或内容格式与 URL 后缀不符时可正确命中已下载缓存）；②**下载内容按文件头嗅探真实格式**（`sniffExt`：OggS/RIFF-WAVE/ID3/MPEG 帧同步/flac/ftyp）——扩展名不定或无扩展名的流式/直链 URL 会按真实内容落到 `<hash>.mp3/.ogg/.wav`，`Sound.createStream` 才能按扩展名正确选择解码器（此前被兜底写成 `.ogg` 而内容实为 mp3 时解码失败→「不能播放外部音频」）；本地复制副本、分块收齐（`finalizeCache`）同样先嗅探再落盘，并 `evictHashVariants` 清理同名旧扩展残留；③`beginPlayback` 的可解码守卫由「URL/本地源的原始字符串」改为「最终可播放文件路径」（本地文件名与内容格式不符也能正确解禁/禁播）；本地曲目时长在无 ASCII 缓存时按限大小内顺手复制出副本再读取，非 ASCII 路径也能立即显示时长；**音量范围扩至 0–1000%（对数滑杆）**、**音高范围 0.1–10x（对数滑杆）**，原生 clamp 同步放宽；**新增倒放**：开关后播放进度按帧时长×实际速率反向回退（近似倒放），回到开头自动停止，开关持久化，倒放期间自动推进/A-B/切曲逻辑被 seek 守卫天然屏蔽 |
| （待定版） | **UI 布局与自适应修复**：循环模式按钮改自适应（弹窗底部不再 uniform 等宽、悬浮条新增循环快捷按钮，宽随文字最短文案自适应、禁用换行、字缩 0.85）；六个循环模式文案统一前缀与符号（Loop:/单曲即停等，不再中英混杂冒号）；**弹窗「现在播放」曲名与悬浮条曲名列改自适应宽度**（`MarqueeLabel` 新增 `maxPref` 上限：`getPrefWidth` 截取上限，cell 用 `growX` 占满可用宽度，文本真正超过可用宽度才开始滚动）——解决「长度足够却仍轮回滚动、顶部曲名空间没充分利用」；歌曲列表行重排：类型改为定宽右对齐、**时长与文件大小拆成两个独立固定宽列**（52/66px 右对齐）各行严格对齐；导入界面改图标+按钮（图片+文案拼接，兼容本 arc 无「图标+文案」TextButton 构造器），背景标签防换行、宽度归一；**「当前专辑筛选下导入新曲自动归入当前专辑」**（本地/URL 导入完成即 addTrackHashToAlbum，导入界面/URL 输入框提示将归入哪专辑）；重置悬浮条位置按钮加宽（120→150px）、字缩 1 防截断；**歌曲列表长歌名滚动时不再被右侧列遮挡**：滚动间隙加大（48→64px）、进入/离开副本间留干净空白，配合类型/时长/大小严格分列右对齐、曲名列与信息列之间留白 10px |
| （待定版） | **修复「完全不能播放任何歌」根因——缓存目录不可写**：歌曲缓存放 `Core.files.cache(...)`，其 base 跟随进程 CWD——游戏从 `C:\Windows`（System32 或 Start-Process 未指定工作目录）启动时解析为 `C:\Windows\cache\music\`，写入即 `Error writing file: C:\Windows\cache\music\int-game1.ogg (absolute)`，内置曲提取失败、URL/本地缓存全部落不了盘 → 任何歌都无法播放。修复：缓存一律定位到 `Core.settings.getDataDirectory()/cache/music/`（绝对路径、自动建目录），并同步改为中文注释「绝不使用 Core.files.cache」；**循环模式再拆分「随机」**：原随机为队列乱序（不重播），新增「真随机（每次独立抽签，可连播）」共 6 种模式，`cycleLoopMode % 6` + 双语键；**重置悬浮条位置按钮文字不再换行**（`setWrap(false)`）；**长曲名改为滚动循环显示（Marquee）**：悬浮条曲名列、弹窗「现在播放」曲名、弹窗曲目列表行曲名改用 `MusicBar.MarqueeLabel`（文本超出后无缝横向滚动、scissor 剪裁），且均为固定宽列（悬浮条 300px / 现在播放 280px / 曲目行 240px 填充剩余 + 类型·时长·专辑·删除列固定）——无论曲名多长**按钮/悬浮窗宽度不再被顶长**，彻底解决「选歌时长歌名导致按钮变长、悬浮窗变长」 |
| （待定版） | **循环模式扩为 5 种**（关闭/列表/单曲/随机/单曲播完即停）：随机模式用作用域内打乱顺序推进（曲目/专辑增删、作用域切换时重建顺序），单曲播完即停为单曲循环的原子版（播完 stop）；**倍速改为 1/16–16x 对数滑杆**（原 0.1–16x，低端更精细，与悬浮条档位循环一致）；**导入/远端解码守卫**：Soloud 仅内置 ogg/mp3/wav 解码器，flac/m4a/wma/aac/opus 只允许导入与字节共享，禁止本地/远端创建声源与时长探测（`isDecodablePath`+64MB 探测上限）——杜绝 `Sound.createStream` 原生失败导致的进程崩溃；**「暂停后恢复/拖本地进度跳成内置曲」根因修复**：新流式声源可能建源后从未进入播放态（解码失败/文件损坏），此前被 `tickLocal` 当「已播完」静默自动推进到下一首（往往是内置曲）——新增 `voiceEverPlayed` 标记，声源建成即记入，建源后静默窗口内未确认播放过则日志告警并停止（不自动跳曲）；**悬浮条播放/暂停按钮点击即时切换**图标/颜色（不依赖下一帧）；**悬浮条曲名单行省略号**（杜绝长曲名换行撑高顶走按钮与进度条）；**悬浮条倍速/专辑按钮加宽**（64/112px）且禁用内包装换行；**悬浮条进度条改 AB-高亮滑块**：A-B 区间在轨道上以半透明色带直观标出（无区间不画）；**悬浮条位置夹取**：重置位置/窗口缩放后条仍在屏幕可视区（先用 `setPosition` 再用拖动逻辑夹取，杜绝条被夹到屏幕外造成长度观感异常）；**弹窗曲目行重构**：类型（内置/URL/本地）独立定宽右对齐列、时长+大小列对齐，曲名单行省略号占满剩余宽度——长名不再挤压按钮破坏对齐；**弹窗模拟面板数值列加宽**（音量/音高 52px、倍速 64px，倍数如 16.000 不再截断遮挡）；**循环按钮文案**：新增 3/4 两种模式中英文案 |
| （待定版） | **修复恢复播放直接原生崩溃（进程崩溃）**：三次 `hs_err_pid*.log` 崩溃栈均为 `resume() → SoloudBridge.seek → Soloud.idSeek` 在原生 `arc64.dll` 空指针（EXCEPTION_ACCESS_VIOLATION）——`beginPlayback` 刚 `Sound.play()` 创建的新流式声源尚未就绪时立刻 `idSeek` 触发 Soloud 内部锁断言（与已记录的 `Music.create` 非 ASCII 崩溃同族）。修复：恢复播放的 seek 不再同步执行，改记入 `pendingResumeSeek`，由每帧 `tickLocal` 在「声源已确认存活（`isPlaying`）且建源后 ≥0.3s」才应用（复用 `lastSeekAt` 守卫）；停播/切曲/关总开关时清除待应用进度；**下载/收齐完成即刷新弹窗曲目行**（URL 下载完成、本地文件分块收齐后，若弹窗开着则重建曲目行，时长/大小从 `--:--` 立即落位，无需重开弹窗）；**游戏暂停（ESC）进度冻结机制接线**：`pausedByGame` 冻结标记此前从未被置 true（死代码）——文档所述「游戏暂停期间进度冻结、恢复时 seek 回冻结点」从未真正生效，ESC 暂停期间进度条照常虚进/恢复后与音频错位。本版本无 `Trigger.pause/resume` 事件的前提下，用每帧轮询 `state.isPaused()` 边沿检测接线：进入暂停记录冻结点并冻结显示，恢复时 seek 回冻结点（复用 `lastSeekAt` 守卫防恢复瞬间误判播完）；冻结期间 `currentTime()` 返回冻结点，A-B/回绕判定天然惰化 |
| （待定版） | **本地文件分块广播改流式读盘**：原来是 `readBytes()` 把整个本地音频一次性读入内存再分块，大文件（≥50MB）在 Android 上有 OOM 风险——改为持有文件句柄 + 复用 24KB 单块缓冲，每帧按块读盘发送；**在途异步触发不再给失主建声源**：URL 下载完成 / 分块收齐时先校验播放者仍在该曲（`ownerHash` 归属快照，stop 会移除、切曲会更新、玩家离开同步清理），owner 已停止/切曲/离场则跳过建立远程声源，杜绝「关了音乐还听到一声」与离场者声音常驻 |
| （待定版） | **A-B 区间与 LOOP_ONE 无缝回绕**：单曲循环（LOOP_ONE）开启时 Soloud 原生循环会在曲末无痕回绕到 0，若 A-B 区间起止点都在轨道中段，每圈会先播一段区间前的内容再进区间——新增基于上帧进度的回退检测（位置相对上帧回退 >0.5s 且非手动拖动后的 1s 内）把播放无缝拉回 A 点，区间在曲末/恰在 B=曲末的极端场景（原生回绕先于 tick 到达 B 点）同样收敛；**A-B 显示与 hasAb 阈值联动**：两点均已设置但间距未达阈值（≤0.3s）时状态文字明确显示「区间过短未生效（请拉开 A、B 两点）」而非笼统「未设置」；音量为内部调用归一（冗余 native 调用移除） |
| （待定版） | **修复拖动进度「跳歌」与跳至末尾自动切曲**：拖动 seek 一律夹取在轨道末尾前 0.5 秒内（原来直接 seek 到末尾会使流立即结束触发自动推进）；seek 后新增 1 秒静默守卫（`lastSeekAt`），避免流式（外部/URL）声源跳转瞬间被 Soloud 判为「已播完」而误推进下一首——这是外部音乐拖进度跳歌的根因；**音量/音高/倍速三个面板合为一行**；音量/音高滑杆支持外部值回写同步（如广播/悬浮条变更后弹窗不闪旧值）；**按钮文字单行**：悬浮条倍速/专辑按钮与弹窗全部曲目/各专辑/曲名按钮禁止换行，悬浮条倍速按钮加宽（48→56px）显示完整；**悬浮条进度条独立一行加高（14→18px）并上下留白**，避免圆钮越界遮挡上方曲名文字；曲名行下沿留白 2px；**弹窗曲目信息列加宽（92→100px、右贴边距 4px、左侧 8px）**，时长未知时显示 `--:--` 占位符保证各行列对齐一致；**顶部「现在播放」面板实时刷新**（自动推进/暂停后状态图标与曲名即时更新，无需重开弹窗）；停止播放后进度复位为 0 |
| （待定版） | **新增倍速功能**：独立「倍速」控制，与音高叠加为实际 Soloud 播放速率（rate = pitch × speed）；设置界面倍速为**对数滑杆 0.1–16x**（无快捷按钮），悬浮条提供常用档循环按钮（0.5/1/1.5/2/4）；**新增 A-B 区间重复**：设 A 点/设 B 点后播放到 B 自动回转到 A（区间重复），设置界面可设 A/B/清除并显示区间状态，切换曲目自动清除；**修复悬浮条拖动崩溃**：拖动过程中悬浮条被移除/重建后 touchUp 读取 bar 报 NPE（`MusicBar$2.touchUp`）——touchUp 与位置持久化前增加 bar 判空，resetPosition 重置前先移除旧控件，杜绝孤儿重复实例；本地批量导入后确认结果自动重新弹出导入界面便于连续追加；**设置界面按钮单行**：A-B 行按钮加宽避免文字自动换行；**消除设置界面底部空白**：曲目列表 pane 置于底层 `growY()` 填满剩余高度 |
| （待定版） | **新增「专辑」(Album) 功能**：曲目可按专辑分组、按专辑限定「上一首/下一首」与循环作用域；弹窗顶部新增专辑筛选栏（全部/各专辑点选 + 新建/删除专辑按钮）、每行新增专辑归属按钮（弹出加入/移出专辑菜单）；悬浮条新增专辑作用域切换按钮（点按轮换全部→各专辑）；删除曲目时同步清理专辑内引用；**本地文件播放修复补强**：本地（含中文/非 ASCII 路径）文件统一复制为 ASCII 安全缓存 `<hash>.<真实扩展名>` 再播放（Soloud 原生 fopen 无法读非 ASCII Windows 路径）；**游戏暂停进度虚进修复**：游戏自身暂停（ESC）期间 Mindustry 静音流式声源但内部继续计时 → 进度冻结显示，游戏恢复时 seek 回冻结点，进度条不再虚进；**暂停拖动进度修复**：暂停态也可拖动/调整进度（改保存的暂停进度），恢复后从该处继续，进度条随调整不含摇动；**悬浮条「点按生成新实例」修复**：scene 层面条被摘下但未移除时先 `remove` 再置空，避免孤儿重复重建出多实例；**本地导入不退出导入界面**：本地批量导入确认后自动重新弹出导入界面便于连续追加；悬浮条进度条独立一行低高度，避免遮挡曲名/按钮文字 |
| （待定版） | 新增玩家位置音乐播放器：内置/URL/本地三类型曲目、多人按距离 3D 共享、完整播放控制、三入口、启用总开关、本地缓存优先；多人声场修复（Sound 音量模型、延迟注册、PlayerLeave 清理）；本地文件分块改帧速限流发送；分块接收先写 `<hash>.part` 暂存、收齐后才转为正式缓存，防半截文件被当有效缓存；自动推进静默间隔由 60s 修正为 0.5s（原本按帧计误当成秒）；本地 URL mp3/wav 下载前登记扩展名避免落成 `.ogg` 解码失败反复重下；远程声源自然播完后清理防 voices 累积；左右声像（pan）定位；坐标上报间隔 30s→2s 提升 3D 跟随；关闭播放器广播 stop 让其他玩家停止听取；每次播放生成的 Sound 在停止/清理时 dispose 释放原生 Soloud 源防会话期资源泄漏；同 hash 的 URL 下载去重（进行中仅追加回调，防并发重复 Http）；地图切换后若本地仍在播放则重广播当前曲目给新地图玩家，避免远端失去声源；添加曲目输入源不合法（无匹配扩展名等）时弹窗内红色提示并保持开启，不静默关闭；音量/音高滑杆标签列宽一致使其纵向对齐；悬浮条展开态拉宽（470→530）避免长曲名被裁切；悬浮条（右下角音符/迷你控制条）改为可拖动，拖动位置持久化到设置跨会话记忆（收起态整条可拖、展开态前端新增拖动把手，各按钮保持可用）；**本机播放音量由「距离衰减」改为全音量输出**（此前本机在声源处却仍乘以 0.12 的 BASE_VOLUME 导致几乎听不见，即「音乐不能正常播放」根因之一）；弹窗 UI 美化：顶部「现在播放」圆盘+状态面板、上一曲/播放暂停/下一曲图标按钮（播放中高亮）、音量/音高并入面板、底部循环/停止/添加三键、当前曲目行高亮显示 |
| （待定版） | 「音乐仍然没声音」修复补强：①**`Core.audio.set(id, pan, volume)` 参数顺序被传反**（arc 中第2参为 pan、第3参为 volume；此前把音量当 pan、把 pan 当音量，声源在屏幕中央 pan≈0 时音量≈0 → 听不到，即剩余「没声音」根因），已按反编译语义在 refreshVolumes 与 updateRemotePosition 两处改正；②**内置（jar 打包）曲目无真实磁盘路径，Soloud 无法流式读取**（报 `streamLoadFile File not found`）——播放前先提取为真实缓存文件再 createStream（本地与远程播放两条路径都补）；悬浮条**点击/拖动无响应修复**：拖动监听由 `addCaptureListener` 改为普通 `addListener` 并在 touchDown 返回 true 取得触摸焦点（capture 监听会干扰子按钮点击，导致整条点击失效），收起态按钮单击展开、展开态把手拖动整条均恢复；弹窗新增**可拖动选进度条**：显示 m:ss / 总时长，拖动滑块即时 seek（经反射调用 Soloud.idPosition/idSeek 读取/定位流式进度，未播放禁用）；进度/时间标签用 `Styles.outlineLabel`，拖动中不跟随刷新避免回跳 |
